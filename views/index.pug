head
	title #{title}
	meta(name='viewport' content='width=device-width, initial-scale=1')
	link(rel='stylesheet' href='/bootstrap.min.css')

body
	div.page-header.col-md-6.col-md-offset-3.col-sm-8.col-sm-offset-2
		h1 #{person.person} #[small #{message}]
